@model WebApplication8.usuario
@{
    ViewBag.Title = "Register";
}
<picture>
    <source media="(min-width: 601px)" srcset="~/Content/Logo_Moto.png">
    <source media="(max-width: 600px)" srcset="~/Content/Logo_Moto2.png">
    <img src="~/Content/Logo_Moto.png" alt="Moto-Delivery" style="width:100%;">
</picture>
<div class="text-center">
    <h2>@ViewBag.Title</h2>
</div>

    @using (Html.BeginForm("Create","usuarios", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
    {
        @Html.AntiForgeryToken()
        <div class="text-center">
            <h4>Registrar nueva cuenta</h4>
</div>

            @Html.ValidationSummary("", new { @class = "text-danger" })

            <div>
                <div class="form-group">
                    @Html.LabelFor(m=>m.email,  new { @class = "col-md-4 control-label" })
                    <div class="col-md-5">
                        @Html.TextBoxFor(m => m.email, new { @class = "form-control" })
                    </div>
                </div>

                <div class="form-group">
                    @Html.LabelFor(m => m.contrasena,  new { @class = "col-md-4 control-label" })
                    <div class="col-md-5">
                        @Html.PasswordFor(m => m.contrasena, new { @class = "form-control" })
                    </div>
                </div>
           @*<div class="form-group">
      @Html.LabelFor(m => m.emailconfirmado, new { @class = "col-md-4 control-label" })
        <div class="col-md-5">
            @Html.PasswordFor(m => m.emailconfirmado, new { @class = "form-control" })
        </div>
    </div> *@ 
                <hr />
                <div class="form-group">
                    <div class="col-md-offset-5 col-md-4">
                        <input type="submit" class="btn btn-default" value="Registrar" />
                    </div>
                </div>
            </div>
            <div>
                @Html.ActionLink("Back to List", "Index")
            </div>
    }

            @section Scripts {
                @Scripts.Render("~/ bundles / jqueryval")
            }
